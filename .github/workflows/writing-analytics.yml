name: Writing Analytics

on:
  pull_request:
    paths:
      - "articles/**"
      - "ideas/published.md"
      - "templates/article.md"
      - "scripts/writing_analytics.py"
      - "tests/**"
      - "docs/WRITING_ANALYTICS.md"
      - "requirements.txt"
      - ".github/workflows/writing-analytics.yml"
  push:
    branches: [main]
    paths:
      - "articles/**"
      - "ideas/published.md"
      - "templates/article.md"
      - "scripts/writing_analytics.py"
      - "tests/**"
      - "docs/WRITING_ANALYTICS.md"
      - "requirements.txt"
      - ".github/workflows/writing-analytics.yml"
  schedule:
    - cron: "15 0 * * *"
  workflow_dispatch:

concurrency:
  group: writing-analytics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run analytics unit tests
        run: python -m unittest discover -s tests -v
      - name: Validate article metadata and analytics rendering
        run: python scripts/writing_analytics.py --check
      - name: Exercise external metric adapters (best effort)
        continue-on-error: true
        run: python scripts/writing_analytics.py --refresh-metrics --check

  refresh:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Run analytics unit tests
        run: python -m unittest discover -s tests -v
      - name: Refresh metrics and generated profile
        env:
          QIITA_TOKEN: ${{ secrets.QIITA_TOKEN }}
        run: python scripts/writing_analytics.py --refresh-metrics
      - name: Commit generated analytics when changed
        run: |
          if [ -z "$(git status --porcelain -- README.md reports/writing-profile.md data/metrics)" ]; then
            echo "No analytics changes"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md reports/writing-profile.md data/metrics
          git commit -m "chore: refresh writing analytics"
          git push
